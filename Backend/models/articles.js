class Article {
    constructor(title, contenu, time_stamp, auteur_id) {
      this.title = title;
      this.content = contenu;
      this.time_stamp = time_stamp;
      this.auteur_id = auteur_id;
    }
  }